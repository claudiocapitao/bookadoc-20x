import react from 'react';

const FinalFormSubmission = () => {
    const text = "Final Form Submission";

    return (
      <div>
        <p>{text}</p>
      </div>
    )
}

export default FinalFormSubmission;